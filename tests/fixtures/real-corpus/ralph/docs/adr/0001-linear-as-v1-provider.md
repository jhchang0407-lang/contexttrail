# Linear as the only v1 provider

Ralph uses Linear as its only ticket-source provider in v1. We keep provider-neutral habits in naming where they are honest, but we do not build a fake shared abstraction before a second provider exists because that would add complexity without proving the boundary.