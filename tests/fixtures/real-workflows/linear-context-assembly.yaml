# Real workflow context-assembly fixture.
#
# Each case represents a Linear-ticket shaped engineering task. Queries are
# natural searches an engineer/agent would issue while picking up the work.
# required_primary docs must be assembled. Each required_support entry is an
# any-of group: one source from the group is enough to satisfy that support need.
# must_include_chunks names the minimum chunk-level evidence needed when the
# primary doc has multiple plausible chunks.

- ticket: THO-228
  title: "PRD-0027 / 27.1.2 SourceProfile nav-field extension"
  queries:
    - "PRD-0027 relational source-level metadata nav fields"
    - "SourceProfile import-time wiring extension"
    - "ADR for AI-assisted setup-time extraction authority"
  required_primary:
    - "docs/prd/0027-relational-source-level-metadata.md"
  required_support:
    - ["docs/adr/0014-agent-assisted-setup-without-truth-promotion.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0027-relational-source-level-metadata.md"
      heading_path:
        - "PRD-0027: Relational Source-Level Metadata (Nav + Link Graph)"
        - "Implementation Decisions"
        - "Slice 27.1 — nav parser"
      rationale: "The implementation ticket needs the committed nav parser field and wiring decision, not just the PRD overview."

- ticket: THO-227
  title: "PRD-0027 / 27.1.1 Nav/sidebar parser module"
  queries:
    - "PRD-0027 nav sidebar parser per-format sub-parsers"
    - "vitepress mkdocs docusaurus nav config parser"
    - "parser synthetic property tests fast-check"
  required_primary:
    - "docs/prd/0027-relational-source-level-metadata.md"
  required_support:
    - ["docs/adr/0014-agent-assisted-setup-without-truth-promotion.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0027-relational-source-level-metadata.md"
      heading_path:
        - "PRD-0027: Relational Source-Level Metadata (Nav + Link Graph)"
        - "Implementation Decisions"
        - "Slice 27.1 — nav parser"
      rationale: "The parser slice is the chunk that names the supported nav formats and extraction boundary."

- ticket: THO-229
  title: "PRD-0027 / 27.1.3 Source-rerank wiring (alias + overview-owner-score)"
  queries:
    - "PRD-0027 source-rerank wiring alias substrate"
    - "overview-owner-score nav landing held-out validation"
    - "source-rerank scoring features nav signals"
  required_primary:
    - "docs/prd/0027-relational-source-level-metadata.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md", "docs/ARCHITECTURE.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0027-relational-source-level-metadata.md"
      heading_path:
        - "PRD-0027: Relational Source-Level Metadata (Nav + Link Graph)"
        - "Implementation Decisions"
        - "Source-rerank consumption (slice 27.3)"
      rationale: "The source-rerank ticket needs the consumption rule chunk where nav evidence is allowed to affect ranking."

- ticket: THO-225
  title: "PRD-0025 / 25.3 BM25F field-weight extension"
  queries:
    - "PRD-0025 structural contextual indexing BM25F"
    - "BM25F field weight title heading body"
    - "chunk reindex structural context schema"
  required_primary:
    - "docs/prd/0025-structural-contextual-indexing.md"
  required_support:
    - ["docs/SCHEMA.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0025-structural-contextual-indexing.md"
      heading_path:
        - "PRD-0025: Structural Contextual Indexing"
        - "Implementation Decisions"
        - "BM25F field-weight changes"
      rationale: "The field-weight ticket hinges on the BM25F weighting chunk, not the broader structural-context proposal."

- ticket: THO-224
  title: "PRD-0025 / 25.2 Chunk-table column extension + FTS5 virtual table"
  queries:
    - "PRD-0025 chunk table column FTS5"
    - "chunk-table virtual table recreation reindex"
    - "schema versioning FTS5 sqlite chunks"
  required_primary:
    - "docs/prd/0025-structural-contextual-indexing.md"
  required_support:
    - ["docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0025-structural-contextual-indexing.md"
      heading_path:
        - "PRD-0025: Structural Contextual Indexing"
        - "Implementation Decisions"
        - "Chunk persistence changes"
      rationale: "The schema ticket needs the chunk-table and FTS persistence decision."

- ticket: THO-223
  title: "PRD-0025 / 25.1 chunk-structural-context extractor"
  queries:
    - "PRD-0025 chunk structural context extractor"
    - "doc_purpose provenance trace chunks"
    - "structural context extractor synthetic property"
  required_primary:
    - "docs/prd/0025-structural-contextual-indexing.md"
  required_support:
    - ["docs/ARCHITECTURE.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0025-structural-contextual-indexing.md"
      heading_path:
        - "PRD-0025: Structural Contextual Indexing"
        - "Implementation Decisions"
        - "New module: src/parse/chunk-structural-context.ts"
      rationale: "The extractor ticket needs the dedicated module contract for structural chunk context."

- ticket: THO-221
  title: "PRD-0024 / 24.2.3 Code-fence entity consumption through alias substrate"
  queries:
    - "PRD-0024 code-fence entity alias consumption"
    - "code_fence_entities source-rerank wiring"
    - "alias substrate source-rerank held-out validation"
  required_primary:
    - "docs/prd/0024-import-time-evidence-extraction.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md", "docs/ARCHITECTURE.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0024-import-time-evidence-extraction.md"
      heading_path:
        - "PRD-0024: Import-Time Evidence Extraction"
        - "Implementation Decisions"
        - "Slice 2 — code-fence entity extraction (locked spec)"
      rationale: "The alias-consumption ticket needs the code-fence entity extraction contract."

- ticket: THO-218
  title: "PRD-0024 / 24.1.3 Source-rerank heading evidence wiring"
  queries:
    - "PRD-0024 import-time evidence heading aliases"
    - "source-rerank heading aliases coverage"
    - "heading_aliases SourceProfile field"
  required_primary:
    - "docs/prd/0024-import-time-evidence-extraction.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0024-import-time-evidence-extraction.md"
      heading_path:
        - "PRD-0024: Import-Time Evidence Extraction"
        - "Implementation Decisions"
        - "Slice 1 — heading exact-match"
      rationale: "The heading-alias rerank ticket needs the committed heading evidence shape."

- ticket: THO-217
  title: "PRD-0024 / 24.1.2 SourceProfile heading_aliases field + import-time wiring"
  queries:
    - "PRD-0024 SourceProfile heading_aliases field"
    - "import-time wiring source profile builder"
    - "ADR setup-time agent-assisted extraction"
  required_primary:
    - "docs/prd/0024-import-time-evidence-extraction.md"
  required_support:
    - ["docs/adr/0014-agent-assisted-setup-without-truth-promotion.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0024-import-time-evidence-extraction.md"
      heading_path:
        - "PRD-0024: Import-Time Evidence Extraction"
        - "Implementation Decisions"
        - "Slice 1 — heading exact-match"
      rationale: "The SourceProfile wiring ticket needs the heading_aliases field decision."

- ticket: THO-216
  title: "PRD-0024 / 24.1.1 extractHeadingAliases extractor"
  queries:
    - "PRD-0024 extractHeadingAliases extractor synthetic property tests"
    - "heading aliases markdown parser H1 H2 H3"
    - "synthetic property tests fast-check extractor"
  required_primary:
    - "docs/prd/0024-import-time-evidence-extraction.md"
  required_support:
    - ["docs/ARCHITECTURE.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0024-import-time-evidence-extraction.md"
      heading_path:
        - "PRD-0024: Import-Time Evidence Extraction"
        - "Solution"
        - "Slice 1 — heading exact-match extraction"
      rationale: "The extractor ticket needs the slice definition that describes exact heading alias extraction."

- ticket: THO-214
  title: "PRD-0023 / 23.3 Source-rerank boosts + flag flip"
  queries:
    - "PRD-0023 path topology source-rerank boosts"
    - "landing index package version match boost"
    - "real-corpus validation flag flip promotion gates"
  required_primary:
    - "docs/prd/0023-import-time-path-topology-compiler.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0023-import-time-path-topology-compiler.md"
      heading_path:
        - "PRD-0023: Import-Time Path Topology Compiler (slice 1)"
        - "Implementation Decisions"
        - "Boost composition"
      rationale: "The source-rerank ticket needs the path-topology boost composition rules."

- ticket: THO-213
  title: "PRD-0023 / 23.2 SourceProfile field extension and import-time wiring"
  queries:
    - "PRD-0023 SourceProfile path topology fields"
    - "path_depth package_segment version_segment SourceProfile"
    - "SourceProfile import-time wiring"
  required_primary:
    - "docs/prd/0023-import-time-path-topology-compiler.md"
  required_support:
    - ["docs/SCHEMA.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0023-import-time-path-topology-compiler.md"
      heading_path:
        - "PRD-0023: Import-Time Path Topology Compiler (slice 1)"
        - "Implementation Decisions"
        - "Code shape"
      rationale: "The SourceProfile field ticket needs the code-shape chunk that names the imported topology fields."

- ticket: THO-208
  title: "PRD-0022 / 22.1 Parent-canonicality close-call tiebreaker"
  queries:
    - "PRD-0022 source-rerank canonicality tiebreaker"
    - "parent canonicality close-call ordering"
    - "source-rerank tiebreaker rules"
  required_primary:
    - "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
      heading_path:
        - "PRD-0022: Source-Rerank Canonicality and Anchor-Basename Tiebreakers"
        - "Solution"
        - "Rule 1: Parent-canonicality close-call tiebreaker"
      rationale: "The ticket is specifically about the parent-canonicality tiebreaker rule."

- ticket: THO-209
  title: "PRD-0022 / 22.2 Anchor-basename-exact close-call tiebreaker"
  queries:
    - "PRD-0022 anchor basename exact tiebreaker"
    - "close-call tiebreaker anchor file path"
    - "source-rerank tiebreaker basename"
  required_primary:
    - "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
      heading_path:
        - "PRD-0022: Source-Rerank Canonicality and Anchor-Basename Tiebreakers"
        - "Solution"
        - "Rule 2: Anchor-basename-exact close-call tiebreaker"
      rationale: "The ticket is specifically about the basename-exact tiebreaker rule."

- ticket: THO-203
  title: "PRD-0021 / 21.2 Policy decision tree with source-rerank fallback"
  queries:
    - "PRD-0021 requirement strength parser conservatism"
    - "applyFacetCoveragePolicy policy decision tree"
    - "source-rerank score fallback selection mode"
  required_primary:
    - "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md", "docs/ARCHITECTURE.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
      heading_path:
        - "PRD-0021: Caller-Anchored Requirements and Parser Inference as Optional Evidence"
        - "Solution"
        - "Policy decision tree"
      rationale: "The policy ticket needs the decision-tree chunk that separates required and optional facet behavior."

- ticket: THO-204
  title: "PRD-0021 / 21.3 Anchor-in-corpus check + imported_source_paths"
  queries:
    - "PRD-0021 anchor in corpus check"
    - "imported_source_paths plumbing retrieval"
    - "anchor file path resolution corpus"
  required_primary:
    - "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
  required_support:
    - ["docs/CORE.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
      heading_path:
        - "PRD-0021: Caller-Anchored Requirements and Parser Inference as Optional Evidence"
        - "Solution"
        - "Anchor-in-corpus check (Q6)"
      rationale: "The imported-source-path ticket needs the anchor-in-corpus semantics."

- ticket: THO-205
  title: "PRD-0021 / 21.4 Excluded-facet authority metadata + provenance trace"
  queries:
    - "PRD-0021 excluded facet authority metadata"
    - "facet provenance trace policy"
    - "facet coverage authority metadata"
  required_primary:
    - "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
  required_support:
    - ["docs/CORE.md", "docs/adr/0014-agent-assisted-setup-without-truth-promotion.md"]
  must_include_chunks:
    - source: "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
      heading_path:
        - "PRD-0021: Caller-Anchored Requirements and Parser Inference as Optional Evidence"
        - "Solution"
        - "Excluded facet authority (Q10)"
      rationale: "The authority-metadata ticket needs the excluded-facet provenance rule."

- ticket: THO-202
  title: "PRD-0021 / 21.1 Route parser-inferred facets to optional_facets"
  queries:
    - "PRD-0021 parser-inferred facets optional_facets"
    - "facet parser conservative inference"
    - "required_facets optional_facets routing"
  required_primary:
    - "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
  required_support:
    - ["docs/CORE.md", "docs/ARCHITECTURE.md"]
  must_include_chunks:
    - source: "docs/prd/0021-requirement-strength-and-parser-conservatism.md"
      heading_path:
        - "PRD-0021: Caller-Anchored Requirements and Parser Inference as Optional Evidence"
        - "Implementation Decisions"
        - "Parser changes"
      rationale: "The parser-routing ticket needs the parser implementation decision chunk."

- ticket: THO-226
  title: "PRD-0027: Relational source-level metadata (nav + link graph)"
  queries:
    - "PRD-0027 relational source-level metadata link graph"
    - "nav graph link graph source profile"
    - "ADR setup-time evidence extraction authority"
  required_primary:
    - "docs/prd/0027-relational-source-level-metadata.md"
  required_support:
    - ["docs/adr/0014-agent-assisted-setup-without-truth-promotion.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0027-relational-source-level-metadata.md"
      heading_path:
        - "PRD-0027: Relational Source-Level Metadata (Nav + Link Graph)"
        - "Solution"
        - "Slice 27.2 — link-graph extractor"
      rationale: "The broader PRD-0027 ticket needs the chunk that defines link-graph extraction."

- ticket: THO-211
  title: "PRD-0023: Import-time path topology compiler"
  queries:
    - "PRD-0023 import-time path topology compiler"
    - "path topology landing index package version"
    - "source-rerank path topology boosts"
  required_primary:
    - "docs/prd/0023-import-time-path-topology-compiler.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/SCHEMA.md"]
  must_include_chunks:
    - source: "docs/prd/0023-import-time-path-topology-compiler.md"
      heading_path:
        - "PRD-0023: Import-Time Path Topology Compiler (slice 1)"
        - "Implementation Decisions"
        - "Code shape"
      rationale: "The whole-PRD ticket still needs the code-shape chunk to implement the topology compiler."

- ticket: THO-207
  title: "PRD-0022: Source-rerank canonicality and anchor-basename tiebreakers"
  queries:
    - "PRD-0022 source-rerank canonicality tiebreakers"
    - "parent canonicality anchor basename close-call"
    - "source-rerank tiebreaker rule anchor"
  required_primary:
    - "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
  required_support:
    - ["docs/prd/0012-source-profile-and-source-rerank.md", "docs/CORE.md"]
  must_include_chunks:
    - source: "docs/prd/0022-source-rerank-canonicality-and-anchor-basename-tiebreakers.md"
      heading_path:
        - "PRD-0022: Source-Rerank Canonicality and Anchor-Basename Tiebreakers"
        - "Solution"
      rationale: "The umbrella ticket needs the rule overview before choosing either close-call tiebreaker."

- ticket: PRD-0026-stub
  title: "PRD-0026 AI contextual augmentation"
  queries:
    - "PRD-0026 AI contextual augmentation"
    - "AI-assisted setup-time augmentation source profile"
    - "ADR-0014 authority boundary AI extraction"
  required_primary:
    - "docs/prd/0026-ai-contextual-augmentation.md"
  required_support:
    - ["docs/adr/0014-agent-assisted-setup-without-truth-promotion.md", "docs/CONTEXT.md"]
  must_include_chunks:
    - source: "docs/prd/0026-ai-contextual-augmentation.md"
      heading_path:
        - "PRD-0026: AI Contextual Augmentation (paper-only — MOOT in current form)"
        - "Implementation Decisions"
        - "LLM-augmented field on the existing six-field BM25F shape"
      rationale: "The stub needs the implementation-decision chunk that explains why this remains paper-only in current form."

- ticket: THO-185-stub
  title: "PRD-0019 Task facet harness and deterministic evidence policy"
  queries:
    - "PRD-0019 task facet harness evidence policy"
    - "facet oracle deterministic evidence"
    - "facet harness coverage policy"
  required_primary:
    - "docs/prd/0019-task-facet-harness-and-deterministic-evidence-policy.md"
  required_support:
    - ["docs/CORE.md", "docs/ARCHITECTURE.md"]
