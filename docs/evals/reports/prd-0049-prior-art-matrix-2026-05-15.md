# PRD-0049 Prior-Art Matrix

This matrix is evidence for shadow evaluation only. It records method fit and
reuse boundaries before any runtime retrieval behavior changes.

| Method | Sources | License | Attribution | Dependency footprint | Local/offline | Code reuse boundary | Candidate recall | Top-3 ordering | Support-cluster usefulness | Cross-repo holdout | Operational boundary |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Aider-style repository map | [source](https://aider.chat/docs/repomap.html)<br>[source](https://github.com/Aider-AI/aider) | Apache-2.0 project license as of inspection; verify before copying code. | Credit Aider repository-map design if adapting the method; preserve notices for any copied snippets. | Tree-sitter/parsing concepts; ContextTrail can adapt from existing code-source facts without adding a parser dependency in the spike. | Local/offline friendly; no hosted service or credentials are required. | Method adaptation only in this PRD; no source code is copied. | Improves owner and symbol-family recall for code-lane misses. | Can keep implementation owners near the top if symbol hits outrank broad support context. | Useful for exact navigation and compact orientation support. | Expected to help repositories with stable exported symbols; neutral where symbols are sparse. | No hosted service / credentials boundary. |
| Continue-style hybrid retrieval/rerank | [source](https://docs.continue.dev/customize/deep-dives/codebase)<br>[source](https://github.com/continuedev/continue) | Apache-2.0 project license as of inspection; verify before copying code. | Credit Continue codebase-indexing and reranking method family if adapted. | Lexical search is already local; embeddings or model rerankers are optional candidates and not CI requirements. | Default path is local/offline; embedding/model paths require explicit cost, latency, and offline guardrails. | Method adaptation only in this PRD; no source code is copied. | Separates generation misses from rerank misses by widening the initial candidate slate. | Expected to improve top-3 ordering when lexical/path/symbol signals agree. | Set-level scoring can keep necessary support with the owner rather than judging chunks alone. | Expected to help retrieval-index holdout gaps if names and paths carry signal. | Hosted service / credentials boundary applies only to optional model rerankers. |
| Sourcegraph/Cody-style multi-source context | [source](https://sourcegraph.com/docs/cody/capabilities/codebase-context)<br>[source](https://github.com/sourcegraph/sourcegraph) | Sourcegraph is Apache-2.0 with enterprise components; inspect exact files before any reuse. | Credit Sourcegraph/Cody multi-source context pattern when adapting the method. | Full system can depend on hosted indexing and code intelligence; ContextTrail should only adapt local graph/search signals here. | Partly local, but hosted service or indexed instance assumptions are out of scope for CI. | Method adaptation only in this PRD; no source code is copied. | Can recover support files through search plus structural relationships. | Risky if graph breadth overwhelms the owner; must improve top-3 usefulness to promote. | Potentially strong for schema/store/import companions with typed reasons. | Expected to help only if the existing code graph has enough signal in the holdout repo. | Hosted service / credentials boundary for Sourcegraph instance or Cody-specific code intelligence. |
| OpenGrok-style search/cross-reference | [source](https://oracle.github.io/opengrok/)<br>[source](https://github.com/oracle/opengrok) | CDDL-1.0 project license; do not copy code into this MIT project without legal review. | Credit OpenGrok search/xref method family if adapting the concept. | Standalone indexing service in the original system; ContextTrail should adapt only narrow local xref ideas. | Can run locally, but service-style indexing is heavier than this spike should require. | Non-compatible license means method adaptation only; code copying is not allowed in this PRD. | Can improve candidate recall for references and import/reindex workflows. | Neutral unless cross-reference reasons are bounded and owner-preserving. | Good candidate for explaining import edge and reverse import edge support. | Expected to help CLI/retrieval-index holdout only if references are not too broad. | Hosted service optionality is out of scope; local index only. |
| REPOFUSE-style fused repository context | [source](https://arxiv.org/abs/2402.14323) | Paper attribution required; implementation license depends on any inspected code artifact. | Cite the paper if using fused repository context ideas in a follow-up design. | Usually assumes model-side context construction and code-completion evaluation machinery. | Conceptual method can be local; model-dependent variants require explicit operational boundaries. | Method adaptation only in this PRD; no source code is copied. | May improve set-level candidate recall but is less directly mapped to ContextTrail's pack contract. | Unproven for top-3 implementation-owner ordering in this repo's eval. | Promising for files useful together, but needs a bounded pack-shaped adapter. | Research signal only until adapted to the existing cross-repo harness. | Large model dependency boundary; not a CI requirement for PRD-0049. |

## Explicit Boundaries

- Hosted service / credentials boundary: not required for CI or local shadow
  adapters.
- Method adaptation only: no OSS source code is copied by PRD-0049.
- Runtime retrieval behavior is unchanged until a later PRD or ADR promotes a
  measured method.
